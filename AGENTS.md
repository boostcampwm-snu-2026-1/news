# Project Instructions

`CLAUDE.md` is the source of truth for this project. Read it first and follow it as the project-level agent instructions.

Codex-specific command mappings:

- `$fix-spec`: use `.agents/skills/fix-spec/SKILL.md`.
- `$next-step`: use `.agents/skills/next-step/SKILL.md`.

This file intentionally stays small so it does not drift from `CLAUDE.md`.
