1. header 만들기 ("뉴스스탠드" 제목과 날짜(년. 월. 일. 요일))
2. ticker 만들기
3. 언론사 더미 데이터 작성
4. 언론사 별 기사 더미 데이터 작성

5. 탭 상태 관리 구현
- 전체 언론사 / 내가 구독한 언론사 탭 전환
- active tab 스타일 반영
- 구독 개수 badge 반영

6. 전체 언론사 그리드 만들기

7. 구독 정보 로컬스토리지에 저장하는 로직 개발

8. 내가 구독한 언론사 그리드 만들기

9. 빈 상태 UI 만들기
- 구독한 언론사가 없을 때 안내 문구 표시
- 마지막 구독 언론사를 해지했을 때 빈 상태로 즉시 전환

10. 그리드 속 뉴스 리스트 보여주는 모드 바꾸는 기능 만들기 (리스트 or block)

11. 페이지네이션 상태 관리 구현
- 현재 탭별 page 상태 관리
- 전체 언론사와 구독 언론사의 page 계산 분리
- 탭 전환 시 page 초기화 여부 결정

12. 그리드 이동을 위한 chevron 만들기

13. 언론사 선택 시 리스트 뷰 만들기

14. 리스트 뷰 6초 주기로 다음 기사로 전환하는 기능 구현

15. 해당 리스트 완료 시 자동으로 다음 언론사로 전환하는 기능 구현

16. 타이머 cleanup 처리
- ticker interval cleanup
- 리스트 뷰 progress interval cleanup
- 컴포넌트 unmount 시 interval 중복 실행 방지

17. reduced motion 대응
- prefers-reduced-motion일 때 ticker / progress 애니메이션 비활성화 또는 단순 전환





공통
0-1. 화면 너비가 줄어들어도 주요 content column이 깨지지 않아야 한다.
0-2. 색상, border, font size가 디자인 토큰과 일치해야 한다.
0-3. hover로 나타나는 구독/해지 버튼은 keyboard focus 상태에서도 나타나야 한다.

Header
1-1. 날짜는 사용자의 현재 날짜 기준으로 년. 월. 일. 요일 형식으로 표시되어야 한다.
    ex) "2026. 01. 14. 수요일"
1-2. 날짜가 한 자리 월/일일 때도 형식이 깨지지 않아야 한다.

Ticker
2-3. ticker에 hover 또는 focus 시 자동 전환이 일시정지되어야 한다.
2-4. prefers-reduced-motion 환경에서는 전환 애니메이션이 비활성화되어야 한다.
2-5. 두 ticker lane이 완전히 같은 타이밍으로 전환되지 않아야 한다.

전체 언론사 그리드
3-4. hover 해제 시 구독/해지 버튼이 사라지고 원래 wordmark가 다시 보여야 한다.
3-5. 구독하기 클릭 시 새로고침 없이 badge count와 구독 상태가 즉시 반영되어야 한다.
3-6. 이미 구독한 언론사는 전체 언론사 탭에서도 해지하기 버튼이 보여야 한다.

내가 구독한 언론사
4-4. 구독한 언론사가 0개일 때 빈 상태 UI가 보여야 한다.
4-5. 구독한 언론사가 24개를 초과하면 다음 페이지 chevron이 활성화되어야 한다.
4-6. 현재 페이지의 마지막 아이템을 해지했을 때 page index가 유효한 값으로 보정되어야 한다.

LocalStorage
5-1. 새로고침 후에도 구독 상태가 유지되어야 한다.
5-2. localStorage에 잘못된 값이 들어있어도 앱이 깨지지 않고 기본값으로 복구되어야 한다.
5-3. 구독/해지 시 중복 id가 저장되지 않아야 한다.

View mode
6-1. grid/list 모드 아이콘의 active 상태가 정확히 표시되어야 한다.
6-2. 모드 전환 시 선택된 탭과 구독 상태가 유지되어야 한다.

Chevron
7-1. 첫 페이지에서는 왼쪽 chevron이 보이지 않아야 한다.
7-2. 마지막 페이지에서는 오른쪽 chevron이 보이지 않아야 한다.
7-3. 중간 페이지에서는 양쪽 chevron이 모두 보여야 한다.
7-4. chevron 클릭 시 현재 탭의 페이지 안에서만 이동해야 한다.

언론사 리스트 뷰
8-3. 리스트 뷰에서 선택한 언론사의 카테고리 탭이 표시되어야 한다.
8-4. 리스트 뷰에서 구독하기/해지하기 버튼이 현재 구독 상태와 동기화되어야 한다.
8-5. 리스트 뷰에서 뒤로 가거나 grid 모드로 돌아갈 수 있어야 한다.

자동 전환
9-1. active category tab의 progress가 6초 동안 0%에서 100%까지 채워져야 한다.
9-2. 6초가 지나면 같은 카테고리의 다음 기사 번호로 이동해야 한다.
9-3. 사용자가 다른 언론사를 선택하면 기존 timer가 초기화되어야 한다.
9-4. 리스트 뷰를 벗어나면 progress timer가 중단되어야 한다.

다음 언론사 전환
10-1. 현재 언론사의 마지막 기사까지 완료되면 다음 언론사로 자동 전환되어야 한다.
10-2. 마지막 언론사까지 완료되면 첫 언론사로 순환해야 한다.
10-3. 구독 탭에서 리스트 뷰를 보고 있다면 다음 언론사는 구독한 언론사 목록 기준이어야 한다.