export const PANEL_W = 736;
const HALF    = PANEL_W / 2;
const BTN_GAP = 25;
export const PEEK_PAD = 48;

export const BASE  = `calc(50% - ${HALF + PANEL_W}px)`;
export const NEXT  = `calc(50% - ${HALF + PANEL_W * 2}px)`;
export const PREV  = `calc(50% - ${HALF}px)`;

export const BTN_LEFT  = `calc(50% - ${HALF + BTN_GAP}px)`;
export const BTN_RIGHT = `calc(50% + ${HALF + BTN_GAP}px)`;
