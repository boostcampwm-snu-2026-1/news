# Verification System (검증 체계)

구현 과정 및 완료 시 프론트엔드 환경에서 발생할 수 있는 엣지 케이스와 UX/UI 품질을 검증하기 위한 체크리스트입니다. PR 또는 커밋 시 아래 항목 중 어떤 것을 검증했는지 기록합니다.

## 1. Logic & Edge Cases
- [x] **구독 0개 상태**: '내가 구독한 언론사' 탭 이동 시, 구독 목록이 0개일 때 빈 화면(또는 안내 메시지)이 적절히 처리되는가?
- [x] **타이머 충돌 검증**: 티커 롤링, 리스트 뷰 프로그레스 바 진행 중 사용자가 탭을 전환하거나 마우스를 올렸다 뗐을 때 메모리 누수나 타이머 중복 실행 현상이 발생하지 않는가?
- [x] **루프(Loop) 동작**: 
  - 티커 데이터의 마지막 요소 다음 첫 번째로 자연스럽게 이동하는가?
  - 리스트 뷰의 마지막 카테고리의 마지막 언론사에서 시간이 만료되었을 때, 다시 첫 번째 카테고리로 롤오버(Rollover) 되는가?

## 2. UI / UX Quality
- [x] **접근성 (Keyboard Nav)**: 마우스 없이 `Tab` 키만으로 구독/해지 버튼으로 포커스가 이동하고, `Enter` 키로 동작을 수행할 수 있는가?
- [x] **모션 최소화 (prefers-reduced-motion)**: 
  - OS에서 애니메이션 줄이기 설정 시, 티커와 프로그레스 바의 애니메이션이 즉각 중지되는가?
- [x] **스케일링 및 해상도**:
  - 기본 레이아웃 폭(1280px) 미만의 화면(랩톱, 태블릿 등)에서 디자인 요소가 깨지지 않고 비율에 맞게 스케일 다운 되는가? (가로 스크롤 이슈 방지)

## 3. 검증 수행 기록

**2026-05-10 (17번 엣지 케이스 검증)**
- ✅ 구독 0개 상태: 그리드/리스트 뷰 모두 "구독한 언론사가 없습니다." 안내 메시지 표시 확인
- ✅ 자동 순환 루프: 6초마다 1/12 → 2/12 순차 전환, 마지막 탭 후 첫 탭으로 롤오버 정상 작동
- ✅ 호버 일시정지: `useCallback` 누락으로 인한 타이머 재실행 버그 수정 후 정상 동작 확인
- ✅ 구독 필터링: 그리드 뷰/리스트 뷰 모두 구독한 언론사만 정확히 표시됨
